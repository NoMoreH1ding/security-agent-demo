# 项目待办事项与改进路线图 🚀

本文件概述了安全代理 (AI-PTA) 的后续开发方向。经过 2026-04-10 的重构，项目已具备良好的模块化基础，接下来的重点是**智能化工具链**、**工具集扩展**与**安全边界控制**。

---

## ✅ 已完成 (Completed)
- [x] **发现-验证-报告三段式架构**：实现了 Analysis, Verification, Reporting 的解耦。
- [x] **Observer 结构化同步**：支持实时漏洞提取与状态快照注入。
- [x] **Web 工具链扩充**：集成了 `Nuclei`, `Dirsearch`, `Web Request` 等核心工具。

---

## 1. 漏洞验证与利用增强 (Exploitation & PoC) 💥
**目标：** 提供更有力的证据链，甚至实现自动化的良性利用测试。

- [ ] **SQLmap 深度集成**：针对发现的注入点调用 SQLmap 获取数据库名（良性验证）。
- [ ] **Exploit-DB 实时检索**：自动根据 CVE ID 搜索 PoC 脚本。
- [ ] **证据链持久化**：Observer 自动截取 `web_request` 的回显存入 `evidence/` 目录。

## 2. 智能化漏洞映射与情报增强 🧠
**目标：** 减少对 LLM 训练数据的依赖，引入实时安全情报。

- [ ] **实时 CVE 关联工具**：集成 NIST NVD 或 Vulners API，根据服务版本号实时检索 CVE。
- [ ] **Exploit-DB 深度搜索**：添加搜索公开 PoC/Exploit 的能力，辅助漏洞分析。
- [ ] **NSE 脚本集成优化**：增强 `service_detail_scan`，支持按需运行特定的 Nmap 脚本引擎 (NSE) 脚本。

## 3. 安全合规与边界控制 (Scope Guard) 🛡️
**目标：** 确保 Agent 的行为严格限制在授权范围内，防止“越界”扫描。

- [ ] **目标范围校验 (Target Scoping)**：实现拦截器，确保所有工具调用的目标都在初始指定的 `targets` 列表中。
- [ ] **敏感操作二次确认**：在 HITL 节点中增加针对破坏性操作的显式风险提示。

## 4. 专业报告与数据持久化 📝
**目标：** 生成可供直接交付的安全审计报告。

- [ ] **结构化报告生成器**：创建一个工具，将发现结果导出为 Markdown、PDF 或 JSON 格式。
- [ ] **任务证据链归档**：在 `reports/` 目录下自动按任务 ID 存储工具的原始输出和截图。

## 5. 可观测性与工程化 📈
**目标：** 提升系统的调试效率和成本可控性。

- [ ] **Token 成本统计**：在 `AgentState` 中记录各阶段消耗。
- [ ] **状态快照对比**：实现工具执行前后状态的 Diff 功能。

---
*由 Gemini CLI 更新 - 2026-04-10*
