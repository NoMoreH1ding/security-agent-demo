# 项目待办事项与改进路线图

本文件概述了进一步开发安全代理（AI-PTA）的建议方向，重点在于架构健壮性、工具丰富度和专业报告。

---

## 1. 结构化状态管理 🏗️
**当前状态：** 已实现核心字段，并增加了 Token 压缩与上下文过滤逻辑。
**目标：** 实现特定领域的循环状态以跟踪渗透测试进度。

- [x] **定义结构化状态**：添加 `targets`、`discovered_hosts` 等字段。
- [x] **上下文压缩 (Token 优化)**：实现 `filter_messages` 逻辑，自动截断过长工具输出并保持消息对完整性。
- [x] **提示词边界优化**：明确 Recon/Analysis 节点职责，严禁重复生成报告。


## 2. 多代理/多节点专业化 🤖
**当前状态：** 单个“代理”节点处理从发现到分析的所有事务。
**目标：** 将工作流程拆分为专门的阶段/节点。

- [x] **实现侦察节点**：专门用于 `host_survival` 和 `quick_port_scan`。
- [x] **实现漏洞利用/分析节点**：用于 `service_detail` 和漏洞映射。
- [x] **实现报告节点**：将发现结果合并到最终报告中。
- [ ] **原因**：专业化允许每个节点使用不同的系统提示词，从而在每个阶段获得更高的精度。
- [ ] **如何开始**：在 `core/graph.py` 中创建新的节点函数，并使用 `StateGraph` 定义状态转换。

## 3. 持久化与人机回环 (HITL) ⏸️
**当前状态：** 执行支持基于 `SqliteSaver` 的持久化。
**目标：** 支持长时间运行的任务和手动审批。

- [x] **添加检查点**：使用 `SqliteSaver` 持久化图状态。
- [x] **添加中断**：在执行高影响工具（例如深度扫描或主动利用）之前实现 `human_review` 节点。
- [x] **原因**：现实世界的渗透测试可能需要数小时。检查点允许恢复执行，而 HITL 确保了安全性和范围合规性。
- [x] **如何开始**：从 `langgraph.checkpoint.sqlite` 导入 `SqliteSaver` 并将其传递给 `workflow.compile(checkpointer=...)`。

## 4. 工具丰富与健壮性 🛠️
**当前状态：** 基础的 Nmap、WAFW00F 和 WhatWeb。
**目标：** 扩展工具包并处理边缘情况。

- [ ] **漏洞查询**：添加一个工具来查询 CVE 数据库或搜索引擎以获取已识别的服务版本。
- [ ] **NSE 集成**：增强 `service_detail_scan` 以可选地运行 Nmap 脚本引擎 (NSE) 脚本。
- [ ] **高级 Web 扫描**：集成 `nuclei` 或 `dirsearch` 等工具。
- [ ] **原因**：为代理提供“实时”情报，而不是仅仅依赖 LLM 的训练数据。
- [ ] **如何开始**：使用 `@tool` 装饰器在 `tools/` 中添加新函数，类似于现有函数。

## 5. 专业报告 📝
**当前状态：** 输出主要是基于控制台的 Markdown。
**目标：** 生成结构化的、可直接交付的报告。

- [ ] **结构化报告工具**：创建一个工具，将发现结果写入 `reports/` 目录下的 JSON 或 Markdown 文件中。
- [ ] **原因**：只有当结果易于消费和归档时，自动化才有意义。
- [ ] **如何开始**：创建一个 `ReportingNode`，从状态中提取 `vulnerabilities` 和 `discovered_services` 并将其格式化为模板。

---
*由 Gemini CLI 生成 - 2026-04-06*
